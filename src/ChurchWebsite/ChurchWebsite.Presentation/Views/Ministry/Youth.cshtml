@{
    ViewData["Title"] = "Youth Ministry";
}

<div class="container">

    <h2>@ViewData["Title"]</h2>

    <div class="row">
        <div class="col-md-9">
            <div class="row">
                <div class="col-md-12">
                    <h3 class="event-title-container">Junior High Youth Group</h3>
                    <span class="event-date-container">Youth Grades 6 to 8</span>
                    <span class="event-date-container">(Program runs for 8 week blocks)</span>
                    <p class="event-text-container">
                        We are part of a community based junior high group that focuses on developing strong leadership skills, promoting service within our community and 
                        connecting with our youth grades 6-8. We run this program in conjunction with other local churches and the Norwich Youth For Christ. The program rotates locations, 
                        but is open to all youth in this age range. Program runs for 8 week blocks - see our <a asp-controller="Home" asp-action="Calendar" class="in-paragraph-link">Calendar of Events</a>
                        for next start date.
                    </p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <ul class="nav nav-pills nav-stacked">
                <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "Ministry" && ViewContext.RouteData.Values["Action"].ToString() == "Family" ? "active" : "")" role="presentation"><a asp-controller="Ministry" asp-action="Family">Familly Ministry</a></li>
                <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "Ministry" && ViewContext.RouteData.Values["Action"].ToString() == "Adult" ? "active" : "")" role="presentation"><a asp-controller="Ministry" asp-action="Adult">Adult Ministry</a></li>
                <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "Ministry" && ViewContext.RouteData.Values["Action"].ToString() == "Youth" ? "active" : "")" role="presentation"><a asp-controller="Ministry" asp-action="Youth">Youth Ministry</a></li>
                <li class="@(ViewContext.RouteData.Values["Controller"].ToString() == "Ministry" && ViewContext.RouteData.Values["Action"].ToString() == "Children" ? "active" : "")" role="presentation"><a asp-controller="Ministry" asp-action="Children">Children's Ministry</a></li>
            </ul>
        </div>
    </div>
</div>